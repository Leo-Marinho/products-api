spring:
  datasource:
    driverClassName: org.h2.Driver
    password: ''
    url: jdbc:h2:mem:testdb
    username: sa

  jpa:
    hibernate:
      hbm2ddl:
        auto: create
    show-sql: true

  h2:
    console:
      enabled: true

  profiles:
    active: test

  flyway:
    enabled: false